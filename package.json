{
  "name": "mockgovsim",
  "version": "2.0.0",
  "description": "MockGovSim - Political Simulation Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve -s frontend/dist -p $PORT",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10"
  }
}
