# Configure Gradle to use the appropriate Java version
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Use Java 17 for compilation (matches sourceCompatibility in build.gradle)
java.toolchain.version=17
# Ensure Gradle uses a compatible JVM
org.gradle.warning.mode=all
# Uncomment and update the line below after installing Java 17
